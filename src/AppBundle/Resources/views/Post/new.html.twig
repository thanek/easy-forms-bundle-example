{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Post creation {{knuf}}</h1>

    {{ form(new_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('post') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
